import mongoose,{Schema,Document}from'mongoose';import bcrypt from'bcryptjs';export interface ICustomer extends Document{name:string;email:string;phone:string;password:string;governorate?:string;city?:string;address?:string;comparePassword(p:string):Promise<boolean>}const S=new Schema({name:{type:String,required:true,trim:true},email:{type:String,required:true,unique:true,lowercase:true,trim:true},phone:{type:String,required:true,trim:true},password:{type:String,required:true},governorate:String,city:String,address:String},{timestamps:true});S.pre('save',async function(this:ICustomer){if(!this.isModified('password'))return;this.password=await bcrypt.hash(this.password,await bcrypt.genSalt(10))});S.methods.comparePassword=function(p:string){return bcrypt.compare(p,this.password)};export default mongoose.model<ICustomer>('Customer',S);
