import{useEffect,useState}from'react';import{apiFetch}from'../../lib/api';export default function AdminOrders(){const[o,setO]=useState<any[]>([]);useEffect(()=>{apiFetch('/api/admin/orders').then(d=>setO(d.orders||[])).catch(()=>{})},[]);return <main className="p-8"><h1 className="mb-6 text-3xl font-bold">Orders</h1>{o.map(x=><div key={x._id} className="mb-3 rounded border bg-white p-4">{x.orderNumber} — LE {x.total} — {x.orderStatus}</div>)}</main>}
