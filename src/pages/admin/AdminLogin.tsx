import{useState}from'react';import{useNavigate}from'react-router-dom';import{useAuth}from'../../context/AuthContext';export default function AdminLogin(){const[email,setEmail]=useState('');const[pw,setPw]=useState('');const{login,error,loading}=useAuth();const nav=useNavigate();return <main className="mx-auto max-w-md p-8"><h1 className="mb-6 text-3xl font-bold">Admin Login</h1><form onSubmit={async e=>{e.preventDefault();try{await login(email,pw);nav('/admin/dashboard')}catch{}}} className="space-y-4"><input className="w-full rounded border p-3" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="w-full rounded border p-3" type="password" placeholder="Password" value={pw} onChange={e=>setPw(e.target.value)}/>{error&&<p className="text-red-600">{error}</p>}<button disabled={loading} className="w-full rounded bg-black p-3 text-white">{loading?'Loading...':'Login'}</button></form></main>}
