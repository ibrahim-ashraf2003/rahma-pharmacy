import{useEffect,useState}from'react';import{apiFetch}from'../../lib/api';export default function AdminReviews(){const[r,setR]=useState<any[]>([]);useEffect(()=>{apiFetch('/api/reviews').then(d=>setR(d.reviews||[])).catch(()=>{})},[]);return <main className="p-8"><h1 className="mb-6 text-3xl font-bold">Reviews</h1>{r.map(x=><div key={x._id} className="mb-3 rounded border bg-white p-4">{x.customerName}: {x.comment}</div>)}</main>}
