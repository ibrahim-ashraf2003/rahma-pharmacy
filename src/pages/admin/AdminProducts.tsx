import{useEffect,useState}from'react';import{apiFetch}from'../../lib/api';export default function AdminProducts(){const[p,setP]=useState<any[]>([]);useEffect(()=>{apiFetch('/api/products').then(d=>setP(d.products||[])).catch(()=>{})},[]);return <main className="p-8"><h1 className="mb-6 text-3xl font-bold">Products</h1><div className="grid gap-3">{p.map(x=><div key={x._id} className="rounded border bg-white p-4">{x.name} — LE {x.price}</div>)}</div></main>}
