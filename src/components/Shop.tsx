import{useEffect,useState}from'react';import ProductCard from'./ProductCard';import{apiFetch}from'../lib/api';export default function Shop(){const[p,setP]=useState<any[]>([]);useEffect(()=>{apiFetch('/api/products').then(d=>setP(d.products||[])).catch(()=>{})},[]);return <section className="mx-auto max-w-7xl p-6"><h1 className="mb-8 text-4xl font-bold">Shop</h1><div className="grid grid-cols-2 gap-5 md:grid-cols-4">{p.map(x=><ProductCard key={x._id||x.id} product={x}/>)}</div></section>}
