import{Link}from'react-router-dom';import{useCart}from'../context/CartContext';export default function ProductCard({product}:any){const{addToCart}=useCart();return <article className="overflow-hidden rounded-xl border bg-white"><Link to={`/product/${product._id||product.id}`}><img src={product.image} alt={product.name} className="aspect-square w-full object-cover"/></Link><div className="p-3"><Link to={`/product/${product._id||product.id}`} className="font-semibold">{product.name}</Link><p className="mt-1">LE {Number(product.price||0).toLocaleString()}</p><button onClick={()=>addToCart(product)} className="mt-3 w-full rounded-lg bg-black px-3 py-2 text-white">Add to cart</button></div></article>}
