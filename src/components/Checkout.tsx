import{useCart}from'../context/CartContext';export default function Checkout({onBack}:{onBack?:()=>void}){const{cartItems,totalPrice,clearCart}=useCart();return <section className="mx-auto max-w-3xl p-6"><button onClick={onBack}>← Back</button><h1 className="my-6 text-3xl font-bold">Checkout</h1><div className="space-y-3">{cartItems.map(i=><div key={i._id||i.id}>{i.name} × {i.quantity}</div>)}</div><p className="mt-6 text-xl font-bold">Total: LE {totalPrice.toLocaleString()}</p><button onClick={clearCart} className="mt-4 rounded bg-black px-5 py-3 text-white">Place order</button></section>}
