import{createContext,useContext,useEffect,useState,ReactNode}from'react';
type Theme='light'|'dark';const C=createContext({theme:'light' as Theme,toggleTheme:()=>{}});export function ThemeProvider({children}:{children:ReactNode}){const[theme,setTheme]=useState<Theme>(()=>(localStorage.getItem('adm_theme') as Theme)||'light');useEffect(()=>{document.documentElement.setAttribute('data-theme',theme);localStorage.setItem('adm_theme',theme)},[theme]);return <C.Provider value={{theme,toggleTheme:()=>setTheme(t=>t==='light'?'dark':'light')}}>{children}</C.Provider>}export const useTheme=()=>useContext(C);
